# 🤖 Telegram-бот для маркетплейсов

Аналитика Ozon и Wildberries прямо в Telegram: KPI, алерты, отчёты, роли и доступы.

Интерфейс сообщений и тексты выстроены в стиле предоставленных скринов: тёмная визуальная подача, карточки метрик, блоки KPI и акцентные алерты.

---

## 📁 Структура проекта

```text
src/
├── index.js
├── api/
│   ├── analytics.js
│   ├── ozon.js
│   └── wildberries.js
├── bot/
│   ├── commands.js
│   ├── dashboard.js
│   └── roles.js
├── db/
│   └── database.js
└── scheduler/
    ├── alerts.js
    └── index.js
```

---

## 🚀 Быстрый старт

### 1) Установка

```bash
npm install
```

### 2) Настройка окружения

```bash
cp .env.example .env
```

Заполните минимум:

- `BOT_TOKEN`
- `OWNER_TELEGRAM_ID`

### 3) Запуск

```bash
npm start
```

Для разработки:

```bash
npm run dev
```

---

## 💬 Команды

| Команда | Доступ | Описание |
|---|---|---|
| `/start` | все добавленные | Приветствие и интерфейс в стиле лендинга |
| `/stats` | все добавленные | Дашборд за сегодня |
| `/month` | manager+ | Отчёт за месяц |
| `/stocks` | manager+ | Остатки по товарам |
| `/settings` | owner | Текущие KPI |
| `/setkpi revenue 5000000` | owner | Изменить KPI |
| `/adduser 123456 manager Алексей` | owner | Добавить/обновить пользователя |
| `/removeuser 123456` | owner | Удалить пользователя |
| `/users` | owner | Список пользователей |
| `/help` | все добавленные | Список команд |

---

## 👥 Роли

- `owner` — полный доступ
- `manager` — отчёты + остатки
- `marketer` — дашборд + алерты
- `viewer` — только базовый дашборд

---

## 🔔 Автоматические уведомления

Бот проверяет KPI каждые 2 часа:

- падение конверсии ниже 70% от цели
- расход рекламного бюджета 85%+
- отставание выручки от ожиданий по дате
- низкое количество заказов
- товар в зоне риска

И плановые отчёты:

- 09:00 — утренний отчёт владельцу
- 20:00 — итоговый отчёт менеджерам

---

## 🧪 Режимы данных

- Если заданы `OZON_METRICS_URL`/`WB_METRICS_URL`, бот берёт данные из API.
- Если API не настроены или недоступны, используется демо-режим (бот продолжает работать без падений).

---

## ⚙️ Примечания

- База данных: SQLite (`DB_PATH`, по умолчанию `./data/bot.db`)
- Таймзона cron: `TIMEZONE` (по умолчанию `Europe/Moscow`)
- Публичная регистрация: `ALLOW_PUBLIC_REGISTRATION=true` (опционально)
